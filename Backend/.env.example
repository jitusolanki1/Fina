MONGO_URI=mongodb://localhost:27017/fina_dev
JWT_SECRET=change_this_to_a_long_random_secret
PORT=4000

# AWS S3 (for presigned uploads)
# Set these if you want to enable presigned S3 uploads
# AWS_REGION=us-east-1
# S3_BUCKET=my-bucket-name
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...

# Example: to generate a JWT for quick testing (Node REPL):
# const jwt = require('jsonwebtoken');
# jwt.sign({ sub: '1234', name: 'Test User' }, 'change_this_to_a_long_random_secret');
